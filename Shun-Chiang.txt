

Project: https://codepen.io/ShunChiang2130/pen/eYmKPjX?editors=1100


 1. What is Semantic HTML? 

Elements such as <h1>, <footer>, or <p> which gives meaning to it's content. 
  
2. What is HTML used for? 

It's the skeleton of the code and forms a backbone which all other code sits on for it to be rendered in the DOM.

3. What is an attribute and where do we put it? 

The attribute describes properties of the element it's in and it is found in the opening tag. 

4. What is the h1 tag used for? How many times should I use it on a page?

H1 is used to bolden the main heading and should be used once. 

5. Name two tags that have required attributes

The a tag and the img tag. 
    
6. What do we put in the head of our HTML document? 

We put links to fonts and external sources like css and MDNs as well as the title of the document.
    
7. What is an id? 

An id is an attribute that defines an element. It has priority over class.
    
8. What elements can I add an id to? 

You can add an id to any element.
   
9. How many times can I use the same id on a page?

Only once  
    
10. What is a class? 

A class is an attribute that names one or a set of like elements.
    
11. What elements can I add a class to? 

You can add a class to any element
    
12. How many times can I use the same class on a page? 

As many times as you desire
    
13. How do I get my link to open in a new tab?

You set the target attribute of your a tag with a target="_blank".

    
14. What is the alt attribute used for? 

It specifies an alternate text for an image, if the image cannot be displayed
    
15. How do I reference an id?

You can reference it using a #.
    
16. What is the difference between a section and a div

A section tag is semantic and denotes a group of something set by a common theme, a div is non semantic and doesn't have any meaning besides having a container.

    
17. What is CSS used for? 

CSS is used to style html.
    
18. How to we select an element? Example - every h2 on the page

You select it by writing the element name without anything. Ex: <h2> would be h2{}.

    
19. What is the difference between a class and an id? - Give me an example of when I might use each one.

An id is used to name one element and it overrides class in terms of priority to be rendered in the DOM. You can name as many elements with the same class as you want. You might want to use an id to override a style rule you have set using a class selector already.

    
20. How do we select classes in CSS?

With a . Before the class name.

    
21. How do we select a p element with a single class of “human””?

 p.human
    
22. What is a parent child selector? When would this be useful? 

Parent child selectors select elements based on how it's nested and what is nesting it. It's useful in cases where you want to select based on the organization of your offspring elements.
    
23. How do you select all links within a div with the class of sidebar?

div.sidebar li{ }
    
24. What is a pseudo selector?

A selector that denotes a condition like hover, checked, active, or nth-child. It is preceded by : and takes priority over class selectors. 
    
25. What do we use the change the spacing between lines?

Line-height

    
26. What do we use to change the spacing between letters?

Letter-spacing

    
27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?

You would specify Uppercase after text-transform
    
28. How do I add a 1px border around my div that is dotted and black?

Border: 1px dotted black

    
29. How do I select everything on the page? 

Using the universal selector *

    
30. How do I write a comment in CSS?

Using /* */
    
31. How do I find out what file I am in, when I am using the command line? 

The current line before the flashing cursor shows you where you are in the directory

    
32. Using the command line - how do I see a list of files/folders in my current folder?

Input ls
    
33. How do I remove a file via the command line? Why do I have to be careful with this?

With the command rm. You have to be cautious because the command line has minimal safeguards and deletes without going into the recycling bin.
 
    
34. Why should I use version control? 

Version control allows you to not only save states but also to branch from each state, track it's history, and allows for collaboration.

    
35. How often should I commit to github?

You should commit new projects to your GitHub as many times as you can

    
36. What is the command we would use to push our repo up to github?

Git remote add origin <repo url> and then git push -u origin master
 
    
37. Walk me through Lambda's git flow.

-we fork the repo from lambda
-add a TL as a collaborator
-we clone the repo
-we branch the repo and add our name
-we add the project to our branch
-we add a TL as a reviewer
-we use the fork as our base and when it is checked off it will be merged back into our master student fork. 

 

Stretch Questions

    
1. What is the difference between an inline element and a block element?

Inline elements like <span> only takes up as much width as necessary while block elements take up the entire new line.
    
2. What happens when an element is positioned absolutely? 
The element gets positioned relative to its ancestor element. 
    
3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 

By using inline-block 
    
4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default

block: div, h1, 
inline: <a> <img> <span>
Block: p, li, div
    
5. In your own words, explain the box model. What is the fix for the box model? 

The box model describes the display properties of an html element which comprises of the content, padding, border, and margin. Each wrapping the next element down. You can use the 
Display: border-box to eliminate the specified width/height being effected by borders.